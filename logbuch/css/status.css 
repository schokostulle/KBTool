/* === status.css === */
/* Globale Status-/Toastmeldungen */

#status-container {
  position: fixed;
  top: 8px;
  right: 8px;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 6px;
  z-index: 1000;
}

.status-message {
  background: #2b2b2b;
  color: #eee;
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 0.9em;
  min-width: 200px;
  max-width: 320px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.4);
  opacity: 0;
  transform: translateY(-10px);
  transition: opacity 0.3s ease, transform 0.3s ease;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.status-message.visible {
  opacity: 1;
  transform: translateY(0);
}

/* Farbliche Abgrenzung je Typ */
.status-message.success { border-left: 4px solid #4caf50; }
.status-message.error   { border-left: 4px solid #f44336; }
.status-message.warning { border-left: 4px solid #ff9800; }
.status-message.info    { border-left: 4px solid #2196f3; }

/* Schließen-Button */
.status-close {
  background: none;
  border: none;
  color: #ccc;
  font-size: 1em;
  cursor: pointer;
  margin-left: 8px;
}

.status-close:hover { color: #fff; }